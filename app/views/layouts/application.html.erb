<!DOCTYPE html>
<html>
  <head>
    <title>Momasy</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500&display=swap" rel="stylesheet">
  </head>

  <body>
    
    <header class="app-header__header">
    <h1 class="app-header__title">
      <%=link_to "Momasy", root_path%>
    </h1>
    
    <nav class="app-header__nav">
      <ul class="nav app-header__site-nav-container">  
        <li class="nav-item app-header__nav_item"><%=link_to "View All Art", products_path%></li> |  
        <li class="nav-item app-header__nav_item"><%=link_to "View All Merchants", merchants_path%></li> 
      </ul>

      <ul class="nav app-header__user-nav-container">
        <% if session[:merchant_id]%>
          <li class="nav-item app-header__nav_item"><%=button_to "Logged in as #{Merchant.find(session[:merchant_id]).username}", merchant_path(session[:merchant_id]), method: :get, class: "btn btn-custom"%></li>
          <li class="nav-item app-header__nav_item"><%=button_to "Logout", logout_path, method: :delete, class: "btn btn-custom"%></li>
        <%else%>
          </li class="nav-item app-header__nav_item"><%=button_to "Merchant Login", github_login_path, method: :get, class: "btn btn-custom"%></li>
        <%end%>
      </ul>
    </nav>
    </header>


    <section class="flash">
      <%flash.each do |name, message|%>
        <div class="<%name%>">
          <%=message%>
        </div>
      <%end%>
    </section>
    

    <section class="container-fluid m5">
    <%= yield %>
    </section>


    <footer class="app-footer__footer">
      About Momasy
    </footer>

  </body>
</html>

